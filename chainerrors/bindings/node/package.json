{
  "name": "@chainfoundry/chainerrors",
  "version": "0.1.0",
  "description": "EVM + Solana error decoder — decode reverts, panics, custom errors, and program failures",
  "main": "index.js",
  "types": "index.d.ts",
  "files": ["index.js", "index.d.ts", "*.node", "README.md"],
  "license": "MIT",
  "keywords": ["ethereum", "evm", "revert", "error", "decoder", "blockchain", "solidity"],
  "repository": {
    "type": "git",
    "url": "https://github.com/DarshanKumar89/chainkit"
  },
  "napi": {
    "name": "chainerrors",
    "triples": {
      "defaults": true,
      "additional": [
        "x86_64-unknown-linux-musl",
        "aarch64-unknown-linux-gnu",
        "aarch64-apple-darwin"
      ]
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi build --platform --release",
    "test": "jest"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
