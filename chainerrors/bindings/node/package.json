{
  "name": "@chainfoundry/chainerrors",
  "version": "0.1.0",
  "description": "EVM revert decoder — decode require(), custom errors, and Panic codes",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "MIT",
  "keywords": ["ethereum", "evm", "revert", "error", "decoder", "blockchain", "solidity"],
  "repository": {
    "type": "git",
    "url": "https://github.com/DarshanKumar89/chainkit"
  },
  "napi": {
    "name": "chainerrors",
    "triples": {
      "defaults": true,
      "additional": [
        "x86_64-unknown-linux-musl",
        "aarch64-unknown-linux-gnu",
        "aarch64-apple-darwin"
      ]
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi build --platform --release",
    "test": "jest"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
