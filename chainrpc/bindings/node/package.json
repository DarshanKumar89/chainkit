{
  "name": "@chainfoundry/chainrpc",
  "version": "0.1.0",
  "description": "Production-grade blockchain RPC transport with retry, circuit breaker, and multi-provider failover",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "MIT",
  "keywords": ["ethereum", "rpc", "blockchain", "web3", "transport", "circuit-breaker", "retry"],
  "repository": {
    "type": "git",
    "url": "https://github.com/DarshanKumar89/chainkit"
  },
  "napi": {
    "name": "chainrpc",
    "triples": {
      "defaults": true,
      "additional": [
        "x86_64-unknown-linux-musl",
        "aarch64-unknown-linux-gnu",
        "aarch64-apple-darwin"
      ]
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "prepublishOnly": "napi build --platform --release",
    "test": "jest"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
