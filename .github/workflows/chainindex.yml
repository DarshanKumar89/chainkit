name: chainindex

on:
  push:
    paths:
      - 'chainindex/**'
      - '.github/workflows/chainindex.yml'
  pull_request:
    paths:
      - 'chainindex/**'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        rust: [stable]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust ${{ matrix.rust }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            chainindex/target
          key: ${{ runner.os }}-cargo-chainindex-${{ hashFiles('chainindex/Cargo.lock') }}

      - name: Format check
        working-directory: chainindex
        run: cargo fmt --check

      - name: Clippy
        working-directory: chainindex
        run: cargo clippy --workspace -- -D warnings

      - name: Test
        working-directory: chainindex
        run: cargo test --workspace

      - name: Build release
        working-directory: chainindex
        run: cargo build --workspace --release
