name: chaincodec

on:
  push:
    paths:
      - 'chaincodec/**'
      - '.github/workflows/chaincodec.yml'
  pull_request:
    paths:
      - 'chaincodec/**'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        rust: [stable]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust ${{ matrix.rust }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            chaincodec/target
          key: ${{ runner.os }}-cargo-chaincodec-${{ hashFiles('chaincodec/Cargo.lock') }}

      - name: Format check
        working-directory: chaincodec
        run: cargo fmt --check

      - name: Clippy
        working-directory: chaincodec
        run: cargo clippy --workspace -- -D warnings

      - name: Test
        working-directory: chaincodec
        run: cargo test --workspace

      - name: Build release
        working-directory: chaincodec
        run: cargo build --workspace --release
