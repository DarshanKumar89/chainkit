name: chainrpc

on:
  push:
    paths:
      - 'chainrpc/**'
      - '.github/workflows/chainrpc.yml'
  pull_request:
    paths:
      - 'chainrpc/**'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        rust: [stable]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust ${{ matrix.rust }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            chainrpc/target
          key: ${{ runner.os }}-cargo-chainrpc-${{ hashFiles('chainrpc/Cargo.lock') }}

      - name: Format check
        working-directory: chainrpc
        run: cargo fmt --check

      - name: Clippy
        working-directory: chainrpc
        run: cargo clippy --workspace -- -D warnings

      - name: Test
        working-directory: chainrpc
        run: cargo test --workspace

      - name: Build release
        working-directory: chainrpc
        run: cargo build --workspace --release
